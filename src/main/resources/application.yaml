server:
  port: 8080

spring:
  application:
    name: HtpsWebSite-Kotlin
  
  datasource:
    url: jdbc:sqlite:data/mydb.sqlite
    driver-class-name: org.sqlite.JDBC

logging:
  level:
    root: INFO
    com.htps: INFO
    org.springframework: INFO

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: htps-death-sky-secret-dskfnvlndjsdipfurhg129318r9y4htueajfo*(&(@*!
  expiration: 3600000  # 毫秒，1小时